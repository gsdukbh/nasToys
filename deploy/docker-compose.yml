services:
  app:
    image: gsdukbh/nastoys:latest
    container_name: nastoys
    restart: always
    network_mode: host
    ports:
        - '8080:8080'
    environment:
        - APP_USERNAME=lee
        - APP_PASSWORD=lijiawei
    volumes:
      - ./data:/app/data
